Template for ClearMaxxApp/ClearMaxx/Services/Secrets.swift  (which is GIT-IGNORED).

Do NOT put real values here. Run ./fetch-secrets.sh to generate the real
Secrets.swift from Google Secret Manager. The generated file looks like:

    enum CMSecrets {
        static let appToken = "<value of secret clearmaxx-app-token>"
    }
